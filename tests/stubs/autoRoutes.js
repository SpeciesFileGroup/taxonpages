// Stands in for `vue-router/auto-routes`, the virtual module the file-based
// router plugin generates from ~/pages. Under test there are no pages; a test
// that needs some mocks this module with vi.mock('vue-router/auto-routes').
export const routes = []
